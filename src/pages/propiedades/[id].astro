---
// Esta página se renderiza en el servidor (SSR) para mostrar propiedades dinámicamente
export const prerender = false;

import { Image } from "astro:assets";
import Layout from "../../layouts/Layout.astro";
import ContactForm from "../../components/react/ContactForm";
import {
  getPropertyById,
  getCatalogProperties,
} from "../../lib/queries/properties";
import { getAllActiveServices } from "../../lib/queries/services";
import { formatPrice } from "../../utils/formatPrice";
import { getTypeLabel, field } from "../../i18n/labels";
import type { PropertyRealEstate, PropertyVehicle } from "../../types/property";

const lang = Astro.locals.lang ?? "es";

// Obtener el ID de la propiedad de los parámetros de la URL
const { id } = Astro.params;

if (!id) {
  return Astro.redirect("/propiedades");
}

const property = await getPropertyById(id, lang);

if (!property) {
  return Astro.redirect("/propiedades");
}

const services = await getAllActiveServices(lang);

const realEstate = Array.isArray(property.property_real_estate)
  ? property.property_real_estate[0]
  : property.property_real_estate;

const vehicle = Array.isArray(property.property_vehicles)
  ? property.property_vehicles[0]
  : property.property_vehicles;

const images =
  property.images && property.images.length > 0
    ? property.images
    : [
        "https://res.cloudinary.com/dnziwpiec/image/upload/v1771916220/roca-business/propiedades/pexels-scottwebb-1029599_dywxnv.jpg",
      ];

function formatNumber(value: number | null | undefined): string {
  if (!value) return "—";
  return new Intl.NumberFormat("es-CR").format(value);
}

const propertyUrl = `https://www.rocabusiness.net/propiedades/${property.id}`;

const propertySchema = property.type === "carro" ? {
  "@context": "https://schema.org",
  "@type": "Product",
  "@id": propertyUrl,
  "name": property.title,
  "description": property.description ?? (lang === "en" ? "Vehicle for sale in Costa Rica" : "Vehículo en venta en Costa Rica"),
  "url": propertyUrl,
  "image": images,
  ...(vehicle?.make ? { "brand": { "@type": "Brand", "name": vehicle.make } } : {}),
  ...(property.price && property.currency ? {
    "offers": {
      "@type": "Offer",
      "price": property.price,
      "priceCurrency": property.currency === "CRC" ? "CRC" : "USD",
      "availability": property.status === "activo" ? "https://schema.org/InStock" : "https://schema.org/SoldOut",
      "seller": { "@id": "https://www.rocabusiness.net/#organization" }
    }
  } : {}),
  ...(vehicle?.year ? { "vehicleModelDate": String(vehicle.year) } : {}),
  ...(vehicle?.mileage != null ? { "mileageFromOdometer": { "@type": "QuantitativeValue", "value": vehicle.mileage, "unitCode": "KMT" } } : {}),
  ...(vehicle?.transmission ? { "vehicleTransmission": vehicle.transmission } : {}),
  ...(vehicle?.fuel_type ? { "fuelType": vehicle.fuel_type } : {})
} : {
  "@context": "https://schema.org",
  "@type": "RealEstateListing",
  "@id": propertyUrl,
  "name": property.title,
  "description": property.description ?? `${getTypeLabel(property.type, lang)} ${lang === "en" ? "in" : "en"} ${realEstate?.canton ?? realEstate?.provincia ?? property.location ?? "Costa Rica"}`,
  "url": propertyUrl,
  "image": images,
  ...(property.price && property.currency ? {
    "offers": {
      "@type": "Offer",
      "price": property.price,
      "priceCurrency": property.currency === "CRC" ? "CRC" : "USD",
      "availability": property.status === "activo" ? "https://schema.org/InStock" : "https://schema.org/SoldOut"
    }
  } : {}),
  "address": {
    "@type": "PostalAddress",
    "addressLocality": realEstate?.canton ?? realEstate?.distrito ?? property.location ?? "Costa Rica",
    "addressRegion": realEstate?.provincia ?? "Guanacaste",
    "addressCountry": "CR"
  },
  ...(realEstate?.bedrooms ? { "numberOfRooms": realEstate.bedrooms } : {}),
  ...(realEstate?.bathrooms ? { "numberOfBathroomsTotal": realEstate.bathrooms } : {}),
  ...(realEstate?.area_m2 ? { "floorSize": { "@type": "QuantitativeValue", "value": realEstate.area_m2, "unitCode": "MTK" } } : {}),
  "broker": { "@id": "https://www.rocabusiness.net/#organization" }
};

const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": field("breadcrumbHome", lang), "item": "https://www.rocabusiness.net/" },
    { "@type": "ListItem", "position": 2, "name": field("breadcrumbProperties", lang), "item": "https://www.rocabusiness.net/propiedades" },
    { "@type": "ListItem", "position": 3, "name": property.title, "item": propertyUrl }
  ]
};
---

<Layout
  title={`${property.title} | Roca Business`}
  description={property.description ||
    `${getTypeLabel(property.type, lang)} ${lang === "en" ? "in" : "en"} ${property.location || "Costa Rica"}. ${field("price", lang)}: ${property.price && property.currency ? formatPrice(property.price, property.currency) : field("onRequest", lang)}`}
>

  <!-- Barra de Navegación Secundaria -->
  <div class="sticky top-20 z-40 border-b border-mid/20 bg-white shadow-sm">
    <div
      class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8"
    >
      <a
        href="/propiedades"
        class="flex items-center gap-2 font-heading text-sm text-deepest/70 transition-colors hover:text-primary"
      >
        <svg
          class="h-4 w-4"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M15 19l-7-7 7-7"></path>
        </svg>
        <span>{field("backLink", lang)}</span>
      </a>
      <button
        type="button"
        onclick={`navigator.share ? navigator.share({title: document.title, url: window.location.href}) : navigator.clipboard.writeText(window.location.href).then(() => alert('${field("linkCopied", lang)}'))`}
        class="flex items-center gap-2 rounded-lg border border-mid/30 bg-surface px-4 py-2 font-heading text-sm text-deepest transition-all hover:border-primary hover:bg-primary/5"
        aria-label={field("shareAria", lang)}
      >
        <svg
          class="h-4 w-4"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <circle cx="18" cy="5" r="3"></circle>
          <circle cx="6" cy="12" r="3"></circle>
          <circle cx="18" cy="19" r="3"></circle>
          <line x1="8.59" y1="13.51" x2="15.42" y2="17.49"></line>
          <line x1="15.41" y1="6.51" x2="8.59" y2="10.49"></line>
        </svg>
        <span class="hidden sm:inline">{field("share", lang)}</span>
      </button>
    </div>
  </div>

  <main class="bg-[#F5F7FA] pb-16 pt-24">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <!-- Header con Título y Precio -->
      <div
        class="mb-6 pt-4 flex flex-row gap-4 sm:flex-row sm:items-start sm:justify-between"
      >
        <div class="flex-1">
          <h1
            class="font-heading text-lg font-extrabold text-deepest sm:text-3xl lg:text-4xl"
          >
            {property.title}
          </h1>
          {
            property.location && (
              <div class="mt-2 flex items-center gap-2 text-sm font-heading text-deepest/60">
                <svg
                  class="h-5 w-5 text-primary"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M12 21s-7-4.35-7-11a7 7 0 1114 0c0 6.65-7 11-7 11z"
                  />
                  <circle cx="12" cy="10" r="2.5" stroke-width="2" />
                </svg>
                <span>{property.location}</span>
              </div>
            )
          }
        </div>
        <div class="text-right">
          <div class="flex items-center justify-end gap-2 mb-1">
            <svg
              class="h-5 w-5 text-primary"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <path
                d="M12 2v20M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"
                stroke-linecap="round"
                stroke-linejoin="round"></path>
            </svg>
            <span class="font-heading text-xs text-primary/70 uppercase"
              >{field("price", lang)}</span
            >
          </div>
          <p
            class="font-heading text-lg font-extrabold text-primary sm:text-4xl"
          >
            {
              property.price && property.currency
                ? formatPrice(property.price, property.currency)
                : field("onRequest", lang)
            }
          </p>
        </div>
      </div>

      <!-- Galería de Imágenes -->
      <!-- Imagen Principal -->
      <div class="mb-4 overflow-hidden rounded-2xl bg-white shadow-md">
        <div class="relative aspect-[2/1] bg-mid/10">
          <img
            id="mainImage"
            src={images[0]}
            alt={`Imagen principal de ${property.title}`}
            width="1400"
            height="700"
            class="h-full w-full object-cover transition-opacity duration-300"
          />
        </div>
      </div>

      <!-- Miniaturas con Scroll Horizontal -->
      {
        images.length > 1 && (
          <div class="mb-8 rounded-2xl bg-white p-4 shadow-md">
            <div class="relative">
              <div
                id="thumbnailScroll"
                class="flex gap-3 overflow-x-auto scroll-smooth pb-2 scrollbar-thin scrollbar-thumb-primary/30 scrollbar-track-transparent hover:scrollbar-thumb-primary/50"
                style="scrollbar-width: thin; scrollbar-color: rgba(19, 64, 116, 0.3) transparent;"
              >
                {images.map((img, idx) => (
                  <button
                    type="button"
                    data-image={img}
                    data-index={idx}
                    class="gallery-thumbnail group relative flex-shrink-0 overflow-hidden rounded-lg border-2 transition-all w-24 h-24 sm:w-28 sm:h-28"
                    class:list={[
                      idx === 0
                        ? "border-primary ring-2 ring-primary/20"
                        : "border-mid/20 hover:border-primary",
                    ]}
                    aria-label={`Ver imagen ${idx + 1}`}
                  >
                    <img
                      src={img}
                      alt={`Vista ${idx + 1} de ${property.title}`}
                      width="150"
                      height="150"
                      class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-110"
                    />
                  </button>
                ))}
              </div>
            </div>
          </div>
        )
      }

      <!-- Visor de Imágenes en Pantalla Completa -->
      <div
        id="imageLightbox"
        class="fixed inset-0 z-[100] hidden items-center justify-center bg-deepest/95 backdrop-blur-sm"
        role="dialog"
        aria-modal="true"
        aria-label="Visor de imágenes"
      >
        <button
          type="button"
          id="lightboxClose"
          class="absolute top-4 right-4 sm:top-6 sm:right-6 z-10 flex h-11 w-11 items-center justify-center rounded-full bg-white/10 text-light transition-colors hover:bg-white/20"
          aria-label="Cerrar visor"
        >
          <svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>

        <button
          type="button"
          id="lightboxPrev"
          class="absolute left-2 z-10 flex h-11 w-11 items-center justify-center rounded-full bg-white/10 text-light transition-colors hover:bg-white/20 sm:left-6 sm:h-12 sm:w-12"
          aria-label="Imagen anterior"
        >
          <svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
          </svg>
        </button>

        <img
          id="lightboxImage"
          src=""
          alt=""
          class="max-h-[85vh] max-w-[90vw] select-none object-contain"
        />

        <button
          type="button"
          id="lightboxNext"
          class="absolute right-2 z-10 flex h-11 w-11 items-center justify-center rounded-full bg-white/10 text-light transition-colors hover:bg-white/20 sm:right-6 sm:h-12 sm:w-12"
          aria-label="Imagen siguiente"
        >
          <svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
          </svg>
        </button>

        <div
          id="lightboxCounter"
          class="absolute bottom-4 left-1/2 -translate-x-1/2 rounded-full bg-white/10 px-4 py-1.5 font-heading text-sm text-light sm:bottom-6"
        >
        </div>
      </div>

      <script>
        let lightboxKeydownHandler: ((e: KeyboardEvent) => void) | null = null;

        function initGallery() {
          const mainImage = document.getElementById(
            "mainImage",
          ) as HTMLImageElement | null;
          const thumbnails = document.querySelectorAll(".gallery-thumbnail");
          const thumbnailScroll = document.getElementById("thumbnailScroll");
          const scrollLeftBtn = document.getElementById("scrollLeft");
          const scrollRightBtn = document.getElementById("scrollRight");

          if (!mainImage) return;

          // Visor de imágenes en pantalla completa: se abre al hacer clic en la
          // imagen principal o en cualquier miniatura. Funciona incluso con una
          // sola foto (sin miniaturas), mostrando solo la imagen principal.
          const galleryImages =
            thumbnails.length > 0
              ? Array.from(thumbnails).map(
                  (t) => t.getAttribute("data-image") || "",
                )
              : [mainImage.src];

          const lightbox = document.getElementById("imageLightbox");
          const lightboxImage = document.getElementById(
            "lightboxImage",
          ) as HTMLImageElement | null;
          const lightboxCounter = document.getElementById("lightboxCounter");
          const lightboxClose = document.getElementById("lightboxClose");
          const lightboxPrev = document.getElementById("lightboxPrev");
          const lightboxNext = document.getElementById("lightboxNext");
          let lightboxIndex = 0;

          const showLightboxImage = () => {
            if (!lightboxImage || galleryImages.length === 0) return;
            lightboxImage.src = galleryImages[lightboxIndex];
            lightboxImage.alt = `Imagen ${lightboxIndex + 1} de ${galleryImages.length}`;
            if (lightboxCounter) {
              lightboxCounter.textContent =
                galleryImages.length > 1
                  ? `${lightboxIndex + 1} / ${galleryImages.length}`
                  : "";
            }
          };

          const openLightbox = (index: number) => {
            if (!lightbox || galleryImages.length === 0) return;
            lightboxIndex = index;
            showLightboxImage();
            lightbox.classList.remove("hidden");
            lightbox.classList.add("flex");
            document.body.style.overflow = "hidden";
            const multiple = galleryImages.length > 1;
            lightboxPrev?.classList.toggle("hidden", !multiple);
            lightboxNext?.classList.toggle("hidden", !multiple);
          };

          const closeLightbox = () => {
            if (!lightbox) return;
            lightbox.classList.add("hidden");
            lightbox.classList.remove("flex");
            document.body.style.overflow = "";
          };

          const showRelative = (delta: number) => {
            if (galleryImages.length === 0) return;
            lightboxIndex =
              (lightboxIndex + delta + galleryImages.length) %
              galleryImages.length;
            showLightboxImage();
          };

          mainImage.style.cursor = "zoom-in";
          mainImage.addEventListener("click", () => {
            const idx = galleryImages.indexOf(mainImage.src);
            openLightbox(idx >= 0 ? idx : 0);
          });

          thumbnails.forEach((thumbnail, idx) => {
            thumbnail.addEventListener("click", () => openLightbox(idx));
          });

          lightboxClose?.addEventListener("click", closeLightbox);
          lightboxPrev?.addEventListener("click", () => showRelative(-1));
          lightboxNext?.addEventListener("click", () => showRelative(1));
          lightbox?.addEventListener("click", (e) => {
            if (e.target === lightbox) closeLightbox();
          });

          // Se quita el listener anterior antes de agregar uno nuevo -- este
          // script corre de nuevo en cada navegación (astro:page-load), pero
          // `document` persiste entre navegaciones con View Transitions, así
          // que sin esto se irían acumulando listeners duplicados.
          if (lightboxKeydownHandler) {
            document.removeEventListener("keydown", lightboxKeydownHandler);
          }
          lightboxKeydownHandler = (e: KeyboardEvent) => {
            if (!lightbox || lightbox.classList.contains("hidden")) return;
            if (e.key === "Escape") closeLightbox();
            else if (e.key === "ArrowLeft") showRelative(-1);
            else if (e.key === "ArrowRight") showRelative(1);
          };
          document.addEventListener("keydown", lightboxKeydownHandler);

          if (thumbnails.length === 0) return;

          // Cambiar imagen principal al hacer clic en miniatura
          thumbnails.forEach((thumbnail) => {
            thumbnail.addEventListener("click", () => {
              const newImageSrc = thumbnail.getAttribute("data-image");
              const index = parseInt(
                thumbnail.getAttribute("data-index") || "0",
              );

              if (mainImage && newImageSrc) {
                // Fade out
                mainImage.style.opacity = "0";

                setTimeout(() => {
                  mainImage.src = newImageSrc;
                  // Fade in
                  mainImage.style.opacity = "1";
                }, 150);

                // Update active thumbnail border
                thumbnails.forEach((t) => {
                  t.classList.remove(
                    "border-primary",
                    "ring-2",
                    "ring-primary/20",
                  );
                  t.classList.add("border-mid/20");
                });
                thumbnail.classList.remove("border-mid/20");
                thumbnail.classList.add(
                  "border-primary",
                  "ring-2",
                  "ring-primary/20",
                );

                // Scroll automático para centrar la miniatura seleccionada
                if (thumbnailScroll) {
                  const thumbElement = thumbnail as HTMLElement;
                  const scrollLeft =
                    thumbElement.offsetLeft -
                    thumbnailScroll.clientWidth / 2 +
                    thumbElement.clientWidth / 2;
                  thumbnailScroll.scrollTo({
                    left: scrollLeft,
                    behavior: "smooth",
                  });
                }
              }
            });
          });

          // Botones de navegación para desktop
          if (scrollLeftBtn && scrollRightBtn && thumbnailScroll) {
            const updateScrollButtons = () => {
              const isAtStart = thumbnailScroll.scrollLeft <= 10;
              const isAtEnd =
                thumbnailScroll.scrollLeft >=
                thumbnailScroll.scrollWidth - thumbnailScroll.clientWidth - 10;

              if (isAtStart) {
                scrollLeftBtn.classList.add("opacity-0", "pointer-events-none");
              } else {
                scrollLeftBtn.classList.remove(
                  "opacity-0",
                  "pointer-events-none",
                );
              }

              if (isAtEnd) {
                scrollRightBtn.classList.add(
                  "opacity-0",
                  "pointer-events-none",
                );
              } else {
                scrollRightBtn.classList.remove(
                  "opacity-0",
                  "pointer-events-none",
                );
              }
            };

            scrollLeftBtn.addEventListener("click", () => {
              thumbnailScroll.scrollBy({
                left: -200,
                behavior: "smooth",
              });
            });

            scrollRightBtn.addEventListener("click", () => {
              thumbnailScroll.scrollBy({
                left: 200,
                behavior: "smooth",
              });
            });

            thumbnailScroll.addEventListener("scroll", updateScrollButtons);
            updateScrollButtons();
          }
        }

        // Inicializar en carga inicial
        document.addEventListener("DOMContentLoaded", initGallery);

        // Reinicializar después de cada navegación con ViewTransitions
        document.addEventListener("astro:page-load", initGallery);
      </script>

      <style>
        /* Custom scrollbar para navegadores webkit */
        #thumbnailScroll::-webkit-scrollbar {
          height: 6px;
        }

        #thumbnailScroll::-webkit-scrollbar-track {
          background: transparent;
        }

        #thumbnailScroll::-webkit-scrollbar-thumb {
          background: rgba(19, 64, 116, 0.3);
          border-radius: 3px;
        }

        #thumbnailScroll::-webkit-scrollbar-thumb:hover {
          background: rgba(19, 64, 116, 0.5);
        }
      </style>

      <!-- Grid de 2 Columnas: Contenido Principal + Contacto -->
      <div
        class:list={[
          "grid grid-cols-1 gap-8",
          property.status !== "vendido"
            ? "lg:grid-cols-[1fr_400px]"
            : "lg:grid-cols-1 max-w-4xl mx-auto",
        ]}
      >
        <!-- Columna Izquierda: Contenido Principal -->
        <div>
          <!-- Descripción -->
          {
            property.description && (
              <section class="mb-6 rounded-2xl bg-white p-6 shadow-md">
                <h2 class="mb-4 font-heading text-xl font-bold text-deepest">
                  {field("description", lang)}
                </h2>
                <p class="whitespace-pre-line font-heading text-sm text-deepest/80 leading-relaxed">
                  {property.description}
                </p>
              </section>
            )
          }

          <!-- Características - Inmuebles -->
          {
            property.type !== "carro" && realEstate && (
              <section class="mb-6 rounded-2xl bg-white p-6 shadow-md">
                <h2 class="mb-6 font-heading text-xl font-bold text-deepest">
                  {field("characteristics", lang)}
                </h2>

                <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
                  {realEstate.bedrooms !== null && realEstate.bedrooms > 0 && (
                    <div class="flex items-center gap-3">
                      <div class="rounded-lg bg-primary/10 p-2.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <path
                            d="M3 9v11c0 .552.448 1 1 1h1c.552 0 1-.448 1-1v-2h12v2c0 .552.448 1 1 1h1c.552 0 1-.448 1-1V9"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M3 9l9-6 9 6"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M21 9v3H3V9"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-lg font-bold text-deepest">
                          {realEstate.bedrooms}
                        </p>
                        <p class="font-heading text-xs text-deepest/60">
                          {field("bedrooms", lang)}
                        </p>
                      </div>
                    </div>
                  )}

                  {realEstate.bathrooms !== null &&
                    realEstate.bathrooms > 0 && (
                      <div class="flex items-center gap-3">
                        <div class="rounded-lg bg-primary/10 p-2.5">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                          >
                            <path
                              d="M9 6v1.5M9 22v-1m0-3v-1m0-3v-1m6-7.5V4a2 2 0 00-2-2h-2a2 2 0 00-2 2v2.5M3 12a9 9 0 0018 0"
                              stroke-linecap="round"
                            />
                          </svg>
                        </div>
                        <div>
                          <p class="font-heading text-lg font-bold text-deepest">
                            {realEstate.bathrooms}
                          </p>
                          <p class="font-heading text-xs text-deepest/60">
                            {field("bathrooms", lang)}
                          </p>
                        </div>
                      </div>
                    )}

                  {realEstate.area_m2 !== null && realEstate.area_m2 > 0 && (
                    <div class="flex items-center gap-3">
                      <div class="rounded-lg bg-primary/10 p-2.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <rect
                            x="3"
                            y="3"
                            width="18"
                            height="18"
                            rx="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M9 3v18M15 3v18M3 9h18M3 15h18"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-lg font-bold text-deepest">
                          {formatNumber(realEstate.area_m2)}
                        </p>
                        <p class="font-heading text-xs text-deepest/60">
                          {field("areaM2", lang)}
                        </p>
                      </div>
                    </div>
                  )}

                  {realEstate.parking_spots !== null &&
                    realEstate.parking_spots > 0 && (
                      <div class="flex items-center gap-3">
                        <div class="rounded-lg bg-primary/10 p-2.5">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                          >
                            <path
                              d="M5 17H3a2 2 0 01-2-2v-2a2 2 0 011-1.732l3.055-1.828A2 2 0 016 9.614V7a3 3 0 013-3h6a3 3 0 013 3v2.614a2 2 0 01.945.826L22 13.268A2 2 0 0123 15v2a2 2 0 01-2 2h-2"
                              stroke-linecap="round"
                              stroke-linejoin="round"
                            />
                            <path
                              d="M5 17a2 2 0 002 2h10a2 2 0 002-2M5 17v-2M19 17v-2"
                              stroke-linecap="round"
                              stroke-linejoin="round"
                            />
                          </svg>
                        </div>
                        <div>
                          <p class="font-heading text-lg font-bold text-deepest">
                            {realEstate.parking_spots}
                          </p>
                          <p class="font-heading text-xs text-deepest/60">
                            {field("parking", lang)}
                          </p>
                        </div>
                      </div>
                    )}

                  {realEstate.hectares !== null && realEstate.hectares > 0 && (
                    <div class="flex items-center gap-3">
                      <div class="rounded-lg bg-primary/10 p-2.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <path
                            d="M4 9V4h5M20 9V4h-5M4 15v5h5M20 15v5h-5"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-lg font-bold text-deepest">
                          {realEstate.hectares}
                        </p>
                        <p class="font-heading text-xs text-deepest/60">
                          {field("hectares", lang)}
                        </p>
                      </div>
                    </div>
                  )}

                  {realEstate.zoning && (
                    <div class="flex items-center gap-3">
                      <div class="rounded-lg bg-primary/10 p-2.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <path
                            d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-lg font-bold text-deepest truncate">
                          {realEstate.zoning}
                        </p>
                        <p class="font-heading text-xs text-deepest/60">
                          {field("zoning", lang)}
                        </p>
                      </div>
                    </div>
                  )}

                  {(property.type === "lote" ||
                    property.type === "finca" ||
                    property.type === "construccion_en_lote") &&
                    realEstate.provincia && (
                      <div class="flex items-center gap-3">
                        <div class="rounded-lg bg-primary/10 p-2.5">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                          >
                            <path
                              d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
                              stroke-linecap="round"
                              stroke-linejoin="round"
                            />
                            <path
                              d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"
                              stroke-linecap="round"
                              stroke-linejoin="round"
                            />
                          </svg>
                        </div>
                        <div>
                          <p class="font-heading text-lg font-bold text-deepest truncate">
                            {realEstate.distrito ||
                              realEstate.canton ||
                              realEstate.provincia}
                          </p>
                          <p class="font-heading text-xs text-deepest/60">
                            {field("location", lang)}
                          </p>
                        </div>
                      </div>
                    )}

                  {(property.type === "lote" ||
                    property.type === "finca" ||
                    property.type === "construccion_en_lote") &&
                    realEstate.vereda && (
                      <div class="flex items-center gap-3">
                        <div class="rounded-lg bg-primary/10 p-2.5">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                          >
                            <path
                              d="M3 9l9-6 9 6v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"
                              stroke-linecap="round"
                              stroke-linejoin="round"
                            />
                            <path
                              d="M9 22V12h6v10"
                              stroke-linecap="round"
                              stroke-linejoin="round"
                            />
                          </svg>
                        </div>
                        <div>
                          <p class="font-heading text-lg font-bold text-deepest truncate">
                            {realEstate.vereda}
                          </p>
                          <p class="font-heading text-xs text-deepest/60">
                            {field("vereda", lang)}
                          </p>
                        </div>
                      </div>
                    )}
                </div>
              </section>
            )
          }

          <!-- Amenidades - Sección Separada -->
          {
            property.type !== "carro" &&
              realEstate &&
              (realEstate.has_kitchen ||
                realEstate.has_living_room ||
                realEstate.cochera ||
                realEstate.has_water ||
                realEstate.has_electricity ||
                realEstate.has_construction ||
                realEstate.vereda ||
                realEstate.zoning) && (
                <section class="mb-6 rounded-2xl bg-white p-6 shadow-md">
                  <div class="flex items-center gap-3 mb-6">
                    <div class="rounded-full bg-primary/10 p-3">
                      <svg
                        class="h-6 w-6 text-primary"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                      >
                        <path
                          d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
                          stroke-linecap="round"
                          stroke-linejoin="round"
                        />
                      </svg>
                    </div>
                    <h2 class="font-heading text-xl font-bold text-deepest">
                      {field("amenities", lang)}
                    </h2>
                  </div>

                  <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
                    {realEstate.has_kitchen && (
                      <div class="flex items-center gap-3 rounded-lg bg-surface/50 p-3 transition-colors hover:bg-surface">
                        <div class="flex-shrink-0 rounded-full bg-primary/10 p-2">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                          </svg>
                        </div>
                        <span class="font-heading text-sm font-medium text-deepest">
                          {field("kitchen", lang)}
                        </span>
                      </div>
                    )}

                    {realEstate.has_living_room && (
                      <div class="flex items-center gap-3 rounded-lg bg-surface/50 p-3 transition-colors hover:bg-surface">
                        <div class="flex-shrink-0 rounded-full bg-primary/10 p-2">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                          </svg>
                        </div>
                        <span class="font-heading text-sm font-medium text-deepest">
                          {field("livingRoom", lang)}
                        </span>
                      </div>
                    )}

                    {realEstate.cochera && (
                      <div class="flex items-center gap-3 rounded-lg bg-surface/50 p-3 transition-colors hover:bg-surface">
                        <div class="flex-shrink-0 rounded-full bg-primary/10 p-2">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                          </svg>
                        </div>
                        <span class="font-heading text-sm font-medium text-deepest">
                          {field("garage", lang)}
                        </span>
                      </div>
                    )}

                    {realEstate.has_water && (
                      <div class="flex items-center gap-3 rounded-lg bg-surface/50 p-3 transition-colors hover:bg-surface">
                        <div class="flex-shrink-0 rounded-full bg-primary/10 p-2">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                          </svg>
                        </div>
                        <span class="font-heading text-sm font-medium text-deepest">
                          {field("water", lang)}
                        </span>
                      </div>
                    )}

                    {realEstate.has_electricity && (
                      <div class="flex items-center gap-3 rounded-lg bg-surface/50 p-3 transition-colors hover:bg-surface">
                        <div class="flex-shrink-0 rounded-full bg-primary/10 p-2">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                          </svg>
                        </div>
                        <span class="font-heading text-sm font-medium text-deepest">
                          {field("electricity", lang)}
                        </span>
                      </div>
                    )}

                    {realEstate.has_construction &&
                      realEstate.construction_type && (
                        <div class="flex items-center gap-3 rounded-lg bg-surface/50 p-3 transition-colors hover:bg-surface">
                          <div class="flex-shrink-0 rounded-full bg-primary/10 p-2">
                            <svg
                              class="h-5 w-5 text-primary"
                              viewBox="0 0 24 24"
                              fill="currentColor"
                            >
                              <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                            </svg>
                          </div>
                          <span class="font-heading text-sm font-medium text-deepest">
                            {field("constructionIn", lang)} {realEstate.construction_type}
                          </span>
                        </div>
                      )}

                    {realEstate.vereda && (
                      <div class="flex items-center gap-3 rounded-lg bg-surface/50 p-3 transition-colors hover:bg-surface">
                        <div class="flex-shrink-0 rounded-full bg-primary/10 p-2">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                          </svg>
                        </div>
                        <span class="font-heading text-sm font-medium text-deepest">
                          {field("veredaPrefix", lang)} {realEstate.vereda}
                        </span>
                      </div>
                    )}

                    {realEstate.zoning && (
                      <div class="flex items-center gap-3 rounded-lg bg-surface/50 p-3 transition-colors hover:bg-surface">
                        <div class="flex-shrink-0 rounded-full bg-primary/10 p-2">
                          <svg
                            class="h-5 w-5 text-primary"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                          </svg>
                        </div>
                        <span class="font-heading text-sm font-medium text-deepest">
                          {field("zoningPrefix", lang)} {realEstate.zoning}
                        </span>
                      </div>
                    )}
                  </div>
                </section>
              )
          }

          <!-- Características - Vehículos -->
          {
            property.type === "carro" && vehicle && (
              <section class="mb-6 rounded-2xl bg-white p-6 shadow-md">
                <h2 class="mb-6 font-heading text-xl font-bold text-deepest">
                  {field("vehicleSpecs", lang)}
                </h2>

                <div class="grid grid-cols-2 gap-6">
                  {vehicle.make && (
                    <div class="flex items-start gap-3 rounded-xl bg-surface p-4">
                      <div class="rounded-lg bg-primary/10 p-2 mt-0.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <path
                            d="M5 17H3a2 2 0 01-2-2v-2a2 2 0 011-1.732l3.055-1.828A2 2 0 016 9.614V7a3 3 0 013-3h6a3 3 0 013 3v2.614a2 2 0 01.945.826L22 13.268A2 2 0 0123 15v2a2 2 0 01-2 2h-2M5 17a2 2 0 002 2h10a2 2 0 002-2M5 17v-2M19 17v-2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-xs text-deepest/60 mb-1">
                          {field("brand", lang)}
                        </p>
                        <p class="font-heading text-sm font-bold text-deepest">
                          {vehicle.make}
                        </p>
                      </div>
                    </div>
                  )}

                  {vehicle.model && (
                    <div class="flex items-start gap-3 rounded-xl bg-surface p-4">
                      <div class="rounded-lg bg-primary/10 p-2 mt-0.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <path
                            d="M12 2L2 7l10 5 10-5-10-5z"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M2 17l10 5 10-5M2 12l10 5 10-5"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-xs text-deepest/60 mb-1">
                          {field("model", lang)}
                        </p>
                        <p class="font-heading text-sm font-bold text-deepest">
                          {vehicle.model}
                        </p>
                      </div>
                    </div>
                  )}

                  {vehicle.year && (
                    <div class="flex items-start gap-3 rounded-xl bg-surface p-4">
                      <div class="rounded-lg bg-primary/10 p-2 mt-0.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <rect
                            x="3"
                            y="4"
                            width="18"
                            height="18"
                            rx="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M16 2v4M8 2v4M3 10h18"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-xs text-deepest/60 mb-1">
                          {field("year", lang)}
                        </p>
                        <p class="font-heading text-sm font-bold text-deepest">
                          {vehicle.year}
                        </p>
                      </div>
                    </div>
                  )}

                  {vehicle.mileage !== null && (
                    <div class="flex items-start gap-3 rounded-xl bg-surface p-4">
                      <div class="rounded-lg bg-primary/10 p-2 mt-0.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <circle
                            cx="12"
                            cy="12"
                            r="10"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M12 6v6l4 2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-xs text-deepest/60 mb-1">
                          {field("mileage", lang)}
                        </p>
                        <p class="font-heading text-sm font-bold text-deepest">
                          {formatNumber(vehicle.mileage)} km
                        </p>
                      </div>
                    </div>
                  )}

                  {vehicle.transmission && (
                    <div class="flex items-start gap-3 rounded-xl bg-surface p-4">
                      <div class="rounded-lg bg-primary/10 p-2 mt-0.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <circle
                            cx="12"
                            cy="12"
                            r="10"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <circle
                            cx="12"
                            cy="12"
                            r="6"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <circle
                            cx="12"
                            cy="12"
                            r="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-xs text-deepest/60 mb-1">
                          {field("transmission", lang)}
                        </p>
                        <p class="font-heading text-sm font-bold text-deepest">
                          {vehicle.transmission}
                        </p>
                      </div>
                    </div>
                  )}

                  {vehicle.fuel_type && (
                    <div class="flex items-start gap-3 rounded-xl bg-surface p-4">
                      <div class="rounded-lg bg-primary/10 p-2 mt-0.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <path
                            d="M3 22v-15c0-1.1.9-2 2-2h8c1.1 0 2 .9 2 2v15"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M15 5h2c.6 0 1 .4 1 1v3l3 3v6c0 .6-.4 1-1 1h-1"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M6 9h6v6H6z"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-xs text-deepest/60 mb-1">
                          {field("fuel", lang)}
                        </p>
                        <p class="font-heading text-sm font-bold text-deepest">
                          {vehicle.fuel_type}
                        </p>
                      </div>
                    </div>
                  )}

                  {vehicle.engine_cc && (
                    <div class="flex items-start gap-3 rounded-xl bg-surface p-4">
                      <div class="rounded-lg bg-primary/10 p-2 mt-0.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <rect
                            x="2"
                            y="6"
                            width="20"
                            height="12"
                            rx="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                          <path
                            d="M7 10v4M12 10v4M17 10v4M2 12h20"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div>
                        <p class="font-heading text-xs text-deepest/60 mb-1">
                          {field("engineCc", lang)}
                        </p>
                        <p class="font-heading text-sm font-bold text-deepest">
                          {formatNumber(vehicle.engine_cc)} cc
                        </p>
                      </div>
                    </div>
                  )}

                  {vehicle.color && (
                    <div class="flex items-start gap-3 rounded-xl bg-surface p-4">
                      <div class="rounded-lg bg-primary/10 p-2 mt-0.5">
                        <svg
                          class="h-5 w-5 text-primary"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="2"
                        >
                          <path
                            d="M12 2.69l5.66 5.66a8 8 0 11-11.31 0z"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                          />
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="font-heading text-xs text-deepest/60 mb-2">
                          {field("color", lang)}
                        </p>
                        <div class="flex items-center gap-3">
                          <div
                            class="h-6 w-6 rounded-lg border-2 border-mid/30 shadow-sm"
                            style={`background-color: ${vehicle.color};`}
                            title={vehicle.color}
                          />
                        </div>
                      </div>
                    </div>
                  )}
                </div>
              </section>
            )
          }
        </div>

        <!-- Columna Derecha: Formulario de Contacto -->
        {
          property.status !== "vendido" && (
            <div>
              <section class="sticky top-48 rounded-2xl bg-white p-6 shadow-lg border border-mid/10">
                <ContactForm
                  client:visible
                  propertyId={property.id}
                  propertyTitle={property.title}
                  propertyPrice={
                    property.price != null && property.currency
                      ? formatPrice(property.price, property.currency)
                      : ""
                  }
                  propertyLocation={property.location || ""}
                  propertyType={getTypeLabel(property.type, lang)}
                  propertyImage={images[0] || ""}
                  services={services}
                  lang={lang}
                />
              </section>
            </div>
          )
        }
      </div>
    </div>
  </main>
  <script type="application/ld+json" set:html={JSON.stringify(propertySchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />
</Layout>
